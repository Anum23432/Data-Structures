// Traverse your name and print its length, indices, and each character.
let name = "Afifa";

// for printing indices..
console.log(name[0]);
// at 0 index we have A..

// for printing length
console.log(name.length);
// length is 5

// now for printing each character we use for loop
// traversal
for(let i=0; i < name.length; i++) {
    console.log(name[i] , "string character");
}
